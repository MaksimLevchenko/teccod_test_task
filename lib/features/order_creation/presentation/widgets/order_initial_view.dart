import 'package:flutter/material.dart';

class OrderInitialView extends StatelessWidget {
  const OrderInitialView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Нажмите кнопку, чтобы создать заказ.'),
    );
  }
}
